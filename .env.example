# Copy to .env and fill in. Loaded via python-dotenv in main.py.
# Preferred (pydantic-settings, AGENT_ prefix):
AGENT_ANTHROPIC_API_KEY=
# Fallback: the Anthropic SDK default is also honored if the above is unset:
# ANTHROPIC_API_KEY=
